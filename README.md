Project:

Number Guessing Game (`number_guessing_game.py`)
- **Description:** A terminal-based game where the computer picks a random number between 1 and 100, and the player receives hints until they guess correctly.
- **Concepts Used:**
  - `while` loops & Boolean flags
  - `if / elif / else` conditional logic
  - Input validation & type casting (`int()`)
  - Python's `random` module
  - 
How to Run

1. Make sure you have Python installed.
2. Run the script in your terminal:
